## 1枚目｜PreSys

### タイトル
**従来｜加工条件を解析条件へ「自分で組み立てる」**

### 動画再生中のセリフ

こちらが、現在PreSysで行っている解析条件設定です。

今回は、絞り加工1工程を例にしています。

まず前提として、LS-DYNAでは、工具の動作、荷重、接触、解析時間などの条件を、それぞれ「キーワード」と呼ばれる設定単位で定義していきます。

実際の業務では毎回ゼロから設定するわけではなく、過去の類似解析のmdlファイルをコピーして、今回の解析に合わせて必要なキーワードの中身を変更しています。

まず今回のメッシュを読み込みます。

ワークについては、SECTIONのキーワードで要素形式を設定し、ELFORMというパラメータに適切な値を設定します。
材料についても、今回使用する材料物性を選択します。

次に工具です。

工具ごとに、固定するのか、Z方向には動けるようにするのか、といった自由度を設定します。
ここでは、あらかじめ用意しているFixedやZ-Freeなどの剛体材料設定から、工具の役割に合ったものを選択します。

続いて工具の動作です。

工具を動かす場合には、PRESCRIBED MOTION RIGIDという、剛体工具に動きを与えるためのキーワードを使用します。

ここではPIDというパラメータで「どの工具を動かすのか」を指定し、VADというパラメータで、今回は変位として動きを与える設定をします。

さらに、実際に工具をどのように動かすかは、DEFINE CURVEという時間と変位の関係を定義するキーワードで設定します。

あらかじめ用意している動作カーブのCSVを読み込み、
SFOには変位方向の倍率として今回の変位量を、
SFAには時間方向の倍率として工具を動かす時間を設定します。

次に、工具に付与するばね荷重です。

LOADのキーワードで荷重条件を設定し、PIDで荷重を与える工具を指定します。

こちらもDEFINE CURVEで、ばね荷重用に用意しているカーブを読み込みます。

そして、工具の動きと荷重が同じ時間軸で作用するように、ここでもSFAに先ほどの工具動作と整合する時間を設定します。

続いて接触条件です。

CONTACT AUTOMATIC SURFACE TO SURFACEという、工具とワークの接触を定義するキーワードを使用します。

ここではMasterとSlaveとして接触させる対象のPIDを指定し、
FS、FDというパラメータに摩擦係数を設定します。

これを、必要な工具とワークの組み合わせごとに繰り返します。

今回は1/4モデルなので、対称条件も設定します。

CONSTRAINED GLOBALという拘束条件を設定するキーワードで軸方向を指定し、今回は対称面が2面あるため、2つ設定します。

最後に解析の終了時間です。

CONTROL TERMINATIONという、解析をいつ終了するかを定義するキーワードのENDTIMに終了時間を設定します。

ここも、先ほど設定した工具動作や荷重の時間と整合する値にしておく必要があります。

これらを設定したうえで、最後に解析条件をエクスポートします。

解析専任者であれば、普段使っているキーワードや、SFA、SFO、FS、FDといったパラメータが何を意味していて、何を設定すればよいのかを把握しているため、比較的すぐに設定できます。

一方で設計者にとっては、まず大量にあるキーワードの中から何を使えばよいのかを知り、その中の各パラメータの意味を理解し、さらに複数の条件が整合するように値を設定する必要があります。


### 動画終了後にフェードインするメッセージ

1. 適切な解析キーワードを **自分で選ぶ**
2. パラメータの意味・設定値を **自分で判断**
3. 条件同士の整合を **自分で確認**

**→ 設計者は「何を、どう設定するか」から迷う**


---

## 2枚目｜新アプリ

### タイトル
**新アプリ｜加工条件から解析条件を「アプリが自動構成」**

### 動画再生中のセリフ

一方、こちらが今回開発したアプリで、先ほどと同じ絞り加工1工程を設定しているところです。

まずメッシュを読み込みます。

ワークについては、対象となるメッシュと使用する材料を選択します。

PreSysでは、SECTIONのELFORMなど解析上必要な設定を意識していましたが、新アプリでは必要な解析条件をアプリ側で設定します。

次に工具です。

それぞれの工具のメッシュを指定し、「動かす工具」なのか「固定工具」なのかを選択します。

PreSysでは、工具の自由度を考えてFixedやZ-Freeなど適切な設定を選んでいましたが、ここでは工具をどう使いたいかを選ぶだけです。

続いて工具の動作です。

先ほどPreSysでは、PRESCRIBED MOTION RIGIDやDEFINE CURVEといったキーワードを選び、PID、VAD、SFA、SFOなどを設定していました。

新アプリでは、それらを意識する必要はありません。

設計者が入力するのは、
「どちらの方向に」
「何mm動かしたいのか」
という加工条件です。

この入力から、必要な工具動作のキーワードやカーブはアプリ側で生成します。

ばね荷重も同様です。

PreSysではLOADやDEFINE CURVEを設定し、工具動作と時間が合うようにSFAを設定していました。

新アプリでは、
バネ定数、
初期たわみ量、
そしてどの工具の動きに追従するのか、
という加工条件を入力します。

工具動作と荷重の時間関係については、アプリ側で整合を取ります。

次に接触です。

PreSysではCONTACTのキーワードで対象のPIDを指定し、FS、FDというパラメータに摩擦係数を設定していました。

新アプリでは、実際の加工条件として「油あり」「油なし」を選択します。

その選択に応じた接触条件や摩擦係数は、アプリ側で設定します。

対称条件についても、PreSysではCONSTRAINED GLOBALのキーワードで軸方向を設定していましたが、ここではモデル上で対称となる平面を選択するだけです。

また、PreSysではCONTROL TERMINATIONのENDTIMとして解析終了時間を設定し、工具動作や荷重条件と時間を合わせる必要がありました。

新アプリでは、こうした条件同士の整合もアプリ側で処理するため、ユーザーが個別に設定する必要はありません。

最後に条件をエクスポートします。

つまり新アプリでは、設計者がLS-DYNAのキーワードやパラメータを覚えて解析条件を組み立てるのではなく、

「どの工具を、どう動かして、どう加工したいか」

という加工条件を入力すれば、それをアプリがLS-DYNAの解析条件へ変換・構成する仕組みにしています。


### 動画終了後にフェードインするメッセージ

1. 解析キーワードではなく **加工条件で設定**
2. 加工に必要な値だけを **入力**
3. 解析条件の生成・整合は **アプリに任せる**

**→ 設計者は「どう加工したいか」に集中できる**