一方、こちらが今回開発したアプリで、先ほどと同じ絞り加工1工程を設定しているところです。

まずメッシュを読み込みます。

PreSysでは、ワークについてSECTIONのELFORMなど、解析上必要な設定を意識していました。

新アプリでは、対象となるワークのメッシュと使用する材料を選択します。
必要な要素設定などはアプリ側で反映します。

次に工具です。

PreSysでは、工具ごとに固定するのか、どの方向に動けるようにするのかを考え、FixedやZ-Freeなど適切な自由度設定を選んでいました。

新アプリでは、それぞれの工具のメッシュを指定して、「固定する工具」なのか「動かす工具」なのかを選択します。

続いて工具の動作です。

PreSysでは、PRESCRIBED MOTION RIGIDやDEFINE CURVEといったキーワードを選び、PID、VAD、SFA、SFOなどのパラメータを設定していました。

新アプリでは、それらを意識する必要はありません。

入力するのは、
「どちらの方向に」
「何mm動かしたいのか」
という加工条件です。

この入力から、必要な工具動作のキーワードやカーブはアプリ側で生成します。

次に、ばね荷重です。

PreSysでは、LOADやDEFINE CURVEのキーワードを設定し、対象工具や荷重カーブを指定したうえで、工具動作と時間が合うようにSFAも設定していました。

新アプリでは、
バネ定数、
初期たわみ量、
どの工具の動きに追従するか、
を指定します。

工具動作と荷重の時間関係については、アプリ側で整合を取ります。

次に接触条件です。

PreSysでは、CONTACTのキーワードを使って接触させる対象のPIDを指定し、FS、FDというパラメータに摩擦係数を設定していました。

新アプリでは、実際の加工条件として「油あり」「油なし」を選択します。

その選択に応じて、必要な接触条件や摩擦係数をアプリ側で設定します。

次に対称条件です。

PreSysでは、CONSTRAINED GLOBALというキーワードで拘束する軸方向を指定していました。

新アプリでは、モデル上で対称となる平面を選択するだけです。

最後に解析終了時間です。

PreSysでは、CONTROL TERMINATIONのENDTIMに終了時間を設定し、工具動作や荷重条件と時間が合っているかを確認する必要がありました。

新アプリでは、こうした条件同士の整合もアプリ側で処理するため、ユーザーが個別に設定する必要はありません。

最後に条件をエクスポートします。

このように、PreSysでは解析キーワードやパラメータを理解して解析条件を組み立てていましたが、新アプリでは、

「どの工具を、どう動かして、どう加工したいか」

という加工条件を入力すれば、必要な解析条件をアプリ側で自動的に構成する仕組みにしています。