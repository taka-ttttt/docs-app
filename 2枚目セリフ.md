一方、こちらが今回開発したアプリで、先ほどと同じ絞り加工1工程を設定しているところです。

先ほどのPreSysでの設定と比較しながら見ていただきます。

まず、今回使用するメッシュをインポートします。

次にワークです。

PreSysでは要素形式などの解析条件を設定していましたが、新アプリでは、ワークに使用するメッシュを割り当てて、材料を選択します。
必要な要素設定はアプリ側で反映します。

次に工具です。

PreSysでは工具ごとに固定・可動などの自由度を設定していましたが、新アプリでは、それぞれの工具にメッシュを割り当てて、「固定する工具」か「動かす工具」かを選択します。

続いて工具動作です。

先ほどはPRESCRIBED MOTION RIGIDやDEFINE CURVEを使い、PID、VAD、SFA、SFOなどを設定していました。

新アプリでは、「どちらの方向に」「何mm動かすか」を入力します。

この加工条件から、必要な工具動作のキーワードやカーブはアプリ側で生成します。

ばね荷重についても、PreSysではLOADやカーブ、時間の設定が必要でしたが、新アプリでは、バネ定数、初期たわみ量、どの工具に追従するかを指定します。

そして、工具動作との時間関係はアプリ側で自動で整合がとられるようになっています。

接触についても、先ほどは対象のPIDやFS、FDを設定していましたが、新アプリでは、実際の加工条件として「油あり」「油なし」を選択します。

対称条件についても、拘束用のキーワードを設定するのではなく、モデル上で対称となる平面を選択します。

最後にエクスポート先の名称を設定して、条件をエクスポートします。

このように、設計者自身がLS-DYNAのキーワードやパラメータを使って解析条件を組み立てるのではなく、「どの工具を、どう動かして、どう加工したいか」という加工条件を入力すれば、必要な解析条件をアプリ側で自動的に構成します。

【動画終了後に表示】

1. 解析キーワードではなく **加工条件で設定**
2. 加工に必要な値だけを **入力**
3. 解析条件の生成・整合は **アプリに任せる**

**→ 設計者は「どう加工したいか」に集中できる**
