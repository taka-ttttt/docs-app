新アプリを見ていただく前に、まずは現在PreSysで実際にどのような解析条件設定をしているのかを見ていただきたいと思います。

今回は、絞り加工1工程を例にしています。

まず前提として、LS-DYNAでは、工具動作、荷重、接触、解析時間などを、それぞれ「キーワード」という単位で設定していきます。

実際の業務では毎回ゼロから作るのではなく、過去の類似解析のmdlファイルをコピーして、今回の条件に合わせて中身を変更しています。

まずメッシュを入れ替え、ワークの要素形式や材料物性、工具の固定・可動などを設定します。

続いて工具動作です。

ここではPRESCRIBED MOTION RIGIDという、工具に動きを与えるキーワードを使います。

PIDでどの工具を動かすか、VADでどのような動きを与えるかを設定し、さらにDEFINE CURVEというキーワードで工具の動きを定義します。

ここでは、SFOには今回の変位量、SFAには工具を動かす時間を設定します。

ばね荷重も別のキーワードで設定し、工具動作と同じ時間になるよう、こちらのSFAも合わせます。

接触については、CONTACTのキーワードで接触させる工具とワークのPIDを指定し、FS、FDというパラメータに摩擦係数を設定します。
これを必要な組み合わせごとに繰り返します。

さらに対称条件を設定し、最後にCONTROL TERMINATIONというキーワードのENDTIMに解析終了時間を設定します。

ここも、先ほど設定した工具動作や荷重と時間が合っている必要があります。

解析専任者であれば、普段使うキーワードや、SFA、SFO、FS、FDといったパラメータの意味を把握しているため、比較的すぐに設定できます。

一方で設計者にとっては、何のキーワードを使うのか、各パラメータに何を設定するのか、さらに条件同士をどう合わせるのか、というところから判断する必要があります。

【動画終了後に表示】

1. 適切な解析キーワードを **自分で選ぶ**
2. パラメータの意味・設定値を **自分で判断**
3. 条件同士の整合を **自分で確認**

**→ 設計者は「何を、どう設定するか」から迷う**
